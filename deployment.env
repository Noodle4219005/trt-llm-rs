# Generated by `trt-llm-rs config --emit-env`. Do not edit: edit
# the TOML and regenerate, or override a single variable on the
# sbatch command line, which still takes precedence.
ISL="${ISL:-4000}"
OSL="${OSL:-200}"
N="${N:-128}"
MAX_SEQ_LEN="${MAX_SEQ_LEN:-4608}"
PREFILL_MAX_NUM_TOKENS="${PREFILL_MAX_NUM_TOKENS:-16384}"
PREFILL_WORKERS="${PREFILL_WORKERS:-4}"
PREFILL_TP="${PREFILL_TP:-2}"
PREFILL_PP="${PREFILL_PP:-1}"
DECODE_WORKERS="${DECODE_WORKERS:-2}"
DECODE_TP="${DECODE_TP:-4}"
CONTEXT_PARALLEL="${CONTEXT_PARALLEL:-1}"
KV_XFER_CONCURRENCY="${KV_XFER_CONCURRENCY:-16}"
KVCACHE_RECEIVE_PARALLEL="${KVCACHE_RECEIVE_PARALLEL:-1}"
MAX_TOKENS_IN_BUFFER="${MAX_TOKENS_IN_BUFFER:-4096}"
XFER_BACKEND="${XFER_BACKEND:-UCX}"
KV_CACHE_DTYPE="${KV_CACHE_DTYPE:-fp8}"
ATTN_BACKEND="${ATTN_BACKEND:-TRTLLM}"
MOE_BACKEND="${MOE_BACKEND:-AUTO}"
ALLREDUCE_STRATEGY="${ALLREDUCE_STRATEGY:-AUTO}"
EXPERT_PARALLEL="${EXPERT_PARALLEL:-1}"
PREFILL_ATTENTION_DP="${PREFILL_ATTENTION_DP:-0}"
CHUNKED_PREFILL="${CHUNKED_PREFILL:-0}"
CUDA_GRAPH_MAX_BATCH="${CUDA_GRAPH_MAX_BATCH:-96}"
TORCH_COMPILE="${TORCH_COMPILE:-0}"
HOST_DISPATCH_SPIN="${HOST_DISPATCH_SPIN:-0}"
BLOCK_REUSE="${BLOCK_REUSE:-false}"
SPEC_DECODE="${SPEC_DECODE:-0}"
SPEC_MODEL="${SPEC_MODEL:-/work/u4063814/hf_cache/eagle3/qwen3-235b-instruct-2507-specforge}"
SPEC_DRAFT_TOKENS="${SPEC_DRAFT_TOKENS:-1}"
